{
  "schema_version": "1.0",
  "task_id": "TASK-999-S2",
  "parent_task_id": "TASK-999",
  "issue": 99,
  "owner_role": "backend",
  "tier": "green",
  "objective": "Fix tests/test_packets.py::test_every_unique_example_required_check_exists_and_runs so it is direction-aware about the canary probe: when docs/agent-org/canary-probe.md exists in the repo the checker must exit 0; when absent it must exit 1 with 'missing' in stdout. Keep the scratch-tree PASS pin and add the mirror scratch-tree FAIL pin so both checker directions stay deterministically pinned regardless of repo state. This unblocks PR #98 (canary) whose ratified packet cannot touch tests/**.",
  "preconditions": [
    { "check": "issue_state", "expect": "open" },
    { "check": "issue_labels", "require": ["task", "test-change-authorized"] }
  ],
  "files_in_scope": ["tests/test_packets.py"],
  "files_out_of_scope": [
    "scripts/**", "tasks/packets/**", "docs/**", "agents/**", ".github/**",
    "contracts/**", "engine/**", "server/**", "web/**", "overlay/**", "bot/**",
    "PRODUCT_DOCTRINE.md", "AGENTS.md"
  ],
  "constraints": [
    "Do not weaken the test: both checker directions must remain pinned (PASS against a valid probe, FAIL 'missing' against an absent probe) via scratch trees.",
    "No changes to scripts/check_canary_probe.py or any packet."
  ],
  "required_checks": [
    "python3 -m pytest tests/test_packets.py -q -k \"not exists_and_runs\""
  ],
  "acceptance_criteria": [
    { "id": "AC-1", "text": "pytest tests/test_packets.py -q passes on a tree without docs/agent-org/canary-probe.md (current main)." },
    { "id": "AC-2", "text": "pytest tests/test_packets.py -q passes on a tree with the probe present (canary/TASK-999-S1 c877430 merged into a local scratch branch)." },
    { "id": "AC-3", "text": "Both checker directions are pinned via scratch trees independent of repo state." },
    { "id": "AC-4", "text": "Diff touches only tests/test_packets.py." }
  ],
  "budgets": {
    "max_attempts": 2,
    "max_files_modified": 1,
    "max_diff_lines": 80,
    "max_tool_calls": 25,
    "max_wall_clock_seconds": 1200,
    "cost_ceiling_usd": null,
    "value_usd": 40.0
  },
  "routing": { "reasoning_effort": "medium", "review_only_if": [] },
  "rollback": "git revert the single test commit; PR #98 returns to red on test/coverage-floor with no other impact."
}
