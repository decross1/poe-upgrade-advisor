{
  "schema_version": "1.0",
  "task_id": "TASK-210-S3",
  "parent_task_id": "TASK-210",
  "issue": 79,
  "owner_role": "frontend",
  "tier": "red",
  "objective": "Replay the end-to-end test suite of parked PR #91 (commit 95b67cb): overlay/test/clipboardPipeline.test.tsx and its snapshot file — mocked-clipboard detect-to-request-to-card snapshots for all four golden verdict fixtures. DEPENDS ON TASK-210-S2 being merged: if overlay/src/clipboardPipeline.ts is absent on main, stop and return status blocked. Snapshots must be produced by the test runner against the merged S2 code, never hand-edited.",
  "preconditions": [
    { "check": "issue_state", "expect": "open" },
    { "check": "issue_labels", "require": ["task"] }
  ],
  "files_in_scope": [
    "overlay/test/clipboardPipeline.test.tsx",
    "overlay/test/__snapshots__/clipboardPipeline.test.tsx.snap"
  ],
  "files_out_of_scope": [
    "overlay/src/**", "overlay/README.md", "contracts/**", "server/**",
    "web/**", "engine/**", "tasks/**", "agents/**", ".github/**",
    "PRODUCT_DOCTRINE.md", "AGENTS.md"
  ],
  "constraints": [
    "Replay parked commit 95b67cb; snapshots regenerated by the runner, never hand-edited.",
    "If a snapshot diverges from 95b67cb's, the divergence must be explained in the PR (it means S2's replay drifted) — do not paper over it.",
    "Green-lane execution (kimi) under the org-side cash wall; this packet's ceiling is its share of the $50 wall from issue #97."
  ],
  "required_checks": [
    "npm --prefix overlay run test",
    "npm --prefix overlay run build"
  ],
  "acceptance_criteria": [
    { "id": "AC-1", "text": "All four golden verdict fixtures have detect-to-card snapshots and the suite is green." },
    { "id": "AC-2", "text": "Diff touches only the two in-scope test files." }
  ],
  "budgets": {
    "max_attempts": 2,
    "max_files_modified": 2,
    "max_diff_lines": 600,
    "max_tool_calls": 30,
    "max_wall_clock_seconds": 1500,
    "cost_ceiling_usd": 25.0,
    "value_usd": 40.0
  },
  "routing": { "reasoning_effort": "medium", "review_only_if": [] },
  "rollback": "git revert the stage commit; only tests are removed, no product behavior changes."
}
