# SPRINT 2026-07-26 → 2026-07-29 (fixture-first / piping-first)

Human directive (2026-07-26): two priorities — the upgrade-checker MVP surface and
Discord piping. Neither waits on the engine. Everything in this sprint runs against
fixtures and contracts; `engine/` and `server/` are explicitly not on the critical path.

## Goal

By TTL 2026-07-29:
1. The verdict card renders all four states from schema-valid fixtures over real HTTP
   (generated client → fixture mock server on the contract's port) — demoable in `web/`.
2. Discord intake piping is live end to end: `/suggest` → fenced intake issue →
   `[DECISION]` relayed back to the origin thread. (Feedback piping — TASK-404,
   #27 — is PARKED needs-redesign under single-channel mode, issue #16.)

Card spec of record: `docs/specs/verdict_card.md`. Fixture home: `contracts/fixtures/`.
Fallback authority if either is late: `contracts/verdict.schema.json` + `contracts/openapi.yaml`.

## Per-role queues (priority order)

### frontend
1. **L1 review of PR #23** (TASK-002, #2) — REVIEW_REQUEST `d3a7fefb` is aging in your
   inbox and blocks backend's whole queue. Run the branch, post evidence, verdict via ledger.
2. **TASK-205 (#25, M)** — fixture-driven verdict card UI in `web/`, all four states
   snapshot-tested, fixtures schema-validated. The sprint centerpiece.
3. **TASK-206 (#26, S)** — fixture mock server at `127.0.0.1:47791/api/v0/diff`;
   generated client; 404/422 paths; overrides round trip.
4. **TASK-204 (#12, S)** — assumptions chip one-tap override against the mock (I3).
5. **TASK-201 (#9, S)** — Tauri-vs-Electron ADR with I6 benchmark data. Unblocks #11.
6. **TASK-203 (#11, M)** — native shell only, per `[PM-REFINEMENT]` on the issue.
   Stretch; do not start shell code before the #9 ADR merges.

### backend
1. **Land TASK-002 (PR #23)** once frontend's verdict arrives — merge via ADR-0003
   (PM executes robot conditions manually).
2. **TASK-401 (#16, S)** — deploy bot + intake round trip, per `[PM-REFINEMENT]` on the
   issue (ledger-vs-label decision, PoB-code-safe scrub, defer + async I/O, tests).
   Digest deferred. Blocked on the human steps in `docs/runbooks/discord_setup.md`.
3. **TASK-101 (#6, M)** — pobcalc spike resumes as soon as 401 is review-ready;
   it is the Phase-1 GO/NO-GO gate and must not starve past this sprint.

(TASK-404 (#27, feedback listener) is PARKED needs-redesign — single-channel
mode, issue #16 — and is off this sprint's active path.)

### pm
1. Keep the merge path moving: execute ADR-0003 manual merges promptly on verdicts
   (PR #23 first); keep inbox at zero unacked.
2. Commit `docs/specs/verdict_card.md` + seed `contracts/fixtures/` (blocker for #25/#26
   if late — the fallback authority clause covers the gap, but close it fast).
3. Finish `docs/runbooks/discord_setup.md`; hand the human-only checklist to the human.
4. Triage any intake issues within the #17 SLA; refine #9 acceptance if frontend asks.

## Dependency edges

```
PR #23 review (FE) ──► TASK-002 merged ──► backend queue unblocked (TASK-401 → 101)
docs/specs/verdict_card.md + contracts/fixtures/ (pm) ──► TASK-205 (#25)
TASK-205 fixtures ──► TASK-206 (#26) ──► TASK-204 (#12)
TASK-201 ADR (#9) ──► TASK-203 (#11, shell only; card arrives from #25)
discord_setup.md human steps ──► TASK-401 (#16)   [TASK-404 (#27) parked — issue #16]
TASK-202 (#10, later) supersedes TASK-206 (mock deleted as part of #10's DoD)
ADR-0003 (accepted) ──► all merges (PM manual, robot conditions verified) until
  TASK-007 (#24) gives distinct identities + robot token
```

No edge crosses roles mid-task: frontend never waits on backend this sprint, and vice
versa, except the PR #23 review (frontend, item 1) and PM merges.

## Definition of "everything is set up"

All boxes checked = sprint infrastructure done; unchecked boxes at TTL are the retro's
first agenda item.

- [ ] PR #23 merged; `main` has hardened CI (ruff hard-fail, tests/, coverage floor).
- [ ] Issues #25, #26, #27 filed with acceptance criteria, TTL 2026-07-29 (done 2026-07-26),
      and `[PM-REFINEMENT]` comments live on #11 and #16 (done 2026-07-26).
- [ ] `docs/specs/verdict_card.md` committed; `contracts/fixtures/` holds schema-valid
      fixtures for all four verdict states + low-confidence + 6-chip cases.
- [ ] `web/` app boots; `check_invariants.py` green against it; snapshot suite green in CI
      with no engine/server present.
- [ ] Mock server answers `/diff` on `127.0.0.1:47791` with contract-valid bodies,
      including 404/422 and the overrides round trip.
- [ ] Bot process running against production Discord guild; one real `/suggest` →
      intake issue → `[DECISION]` → thread relay demonstrated and linked from #16.
- [ ] Human-gated items done or explicitly parked by the human: Discord app/token/invite
      (the Message Content intent is parked with TASK-404/#27 — single-channel mode,
      issue #16), and TASK-007 (#24) identity/token provisioning
      (parked is acceptable — ADR-0003 covers merges meanwhile).
- [ ] Both agent loops alive, zero unacked ledger messages older than one poll interval,
      no dead-letters in `tasks/dead_letter/`.

Revisit-if: engine spike (TASK-101) slips more than 48h behind piping work — then
PM re-sequences the backend queue. (TASK-404 is already parked needs-redesign per
issue #16, so there is nothing left to drop mid-sprint.)
