# Frozen poe.ninja Spark Inquisitor export with Assassin's Mark charge setup.
# Fetch provenance and immutable hashes are recorded in the corpus manifest.
build: engine/corpus/seed/ninja/23-inquisitor-spark.json
preset: bossing
expected:
  - rule_id: config.power_charges_from_generation
    fires: true
  - rule_id: main_skill.most_linked_highest_dps
    resolved: { main_skill: "Spark of the Nova" }
min_confidence: 0.8
