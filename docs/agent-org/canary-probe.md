# Canary probe

This file is the governed dispatch chain's first live proof.

It verifies that a task can pass preflight, run through the dispatcher, satisfy its required check and completion proofs, push a commit, and be acknowledged.
